import {el} from 'redom';

class MainInfoComponent {
  constructor(init_element) {
    this.el = el('div');
  }

  initData(data) {

  }

  update(data) {
    
  }
}

export default MainInfoComponent;
